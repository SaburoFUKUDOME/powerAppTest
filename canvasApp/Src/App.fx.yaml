App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =Set(baseColor,RGBA(100, 149, 237, 1));
        // データベース名を変数に持たせる
        Set(questionList,'仮説構築支援ツール - 問い');
        Set(navigationList,'仮説構築支援ツール - ナビゲーション');
        Set(themeList,'仮説構築支援ツール - テーマ');
        Set(adminList,'仮説構築支援ツール - 管理者リスト');
        Set(loginUserAddres,User().Email);
        //If(Fillter(adminList,adminList.admin_address = loginUserAddres),Set(admin,true),Set(admin,false))

